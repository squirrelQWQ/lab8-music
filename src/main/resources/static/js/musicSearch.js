document.addEventListener("DOMContentLoaded", function() {
    // 获取UpdateSuccess的值
    var UpdateResultValue = document.getElementById("UpdateResultInput").value;

    // 检查是否存在UpdateSuccess消息，如果存在则显示alert
    if (UpdateResultValue) {
        alert(UpdateResultValue);
    }
});
document.addEventListener("DOMContentLoaded", function() {
    // 获取UpdateSuccess的值
    var DeleteResultValue = document.getElementById("DeleteResultInput").value;

    // 检查是否存在UpdateSuccess消息，如果存在则显示alert
    if (DeleteResultValue) {
        alert(DeleteResultValue);
    }
});

document.addEventListener("DOMContentLoaded", function() {
    // 获取UpdateSuccess的值
    var UploadResultValue = document.getElementById("UploadResultInput").value;

    // 检查是否存在UpdateSuccess消息，如果存在则显示alert
    if (UploadResultValue) {
        alert(UploadResultValue);
    }
});

document.addEventListener("DOMContentLoaded", function() {
    // 获取UpdateSuccess的值
    var searchByTitleResultValue = document.getElementById("searchByTitleResultInput").value;

    // 检查是否存在UpdateSuccess消息，如果存在则显示alert
    if (searchByTitleResultValue) {
        alert(searchByTitleResultValue);
    }
});